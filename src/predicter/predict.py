# Placeholder file

import random

def predict_score(team1, team2):
    # Mock prediction — replace with model inference
    score1, score2 = random.randint(0,3), random.randint(0,3)
    return f"{score1} - {score2}"
